# RivuLab Website

这是 RivuLab 的 GitHub Pages 静态网站仓库。

## 本地预览

直接打开 `index.html` 即可查看页面；也可以运行一个本地静态服务器：

```bash
python3 -m http.server 8000
```

然后访问 `http://localhost:8000`。

## 文件结构

- `index.html`：网站首页内容
- `styles.css`：响应式样式
- `script.js`：移动端导航与年份更新
- `assets/`：站点视觉资产
